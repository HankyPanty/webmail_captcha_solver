# webmail_captcha_solver

Cleaned the image of noise, and applied a sobel edge detector on it. 
`still could not successfully train it and hence did not upload the trainer SVM files`
Working on SVM and Training
